- name: Install and run nginx
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: Install nginx
      ansible.builtin.package:
        name: nginx
        state: present
  tasks:
    - name: Start nginx
      ansible.builtin.service:
        name: nginx
        state: present

